package com.metroInsight.hbase;

public class Hbase {

	/*
	 * Hbase Commands:
	 * 1) Deleting all the tables in Hbase via command shell
	 *    echo 'list.each {|t| disable t; drop t}; quit;' | ./bin/hbase shell
	 * 2) 
	 */
	
}
